var TF = (function () {
    "use strict";
    var playersMap = new Map(); // Load from file

    // Public
    return {
        init: function () {
            console.log("init()");
        }
    }
})();

export default TF;